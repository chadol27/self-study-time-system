# 저장소 작업 지침

## 문서 기준

- 구현 전에 이 문서의 구현 정책을 읽고 출결 상태, 시트 구조, 인증, 동시성, 날짜 경계 규칙을 따른다.
- 설치·배포·운영 절차는 `README.md`를 따른다. 문서와 실행 코드가 다르면 `src/appsscript.json`, `.claspignore`, 실제 코드를 우선 확인한다.
- 코드를 수정하면 변경된 동작과 정책을 이 문서에, 설치·배포·운영 영향을 `README.md`에 함께 반영한다.

## 작업 환경

- 이 저장소는 컨테이너 바운드 Google Apps Script 웹 앱이다. 의존성은 `pnpm install`로 설치한다.
- Apps Script에 push되는 manifest, `.gs`, `.html` 파일은 모두 `src/`에 두며 `.clasp.json`의 `rootDir`을 `src`로 유지한다.
- HTML, 인라인 CSS·JavaScript와 `.gs` 파일은 Prettier로 포맷한다. `pnpm format`은 `src/*.html`과 `src/*.gs`를 포맷하고 `pnpm format:check`는 포맷 상태를 검사한다.
- 검사는 `pnpm check`로 실행하며 순서는 `typecheck` → `lint:gs` → `lint:html` → `format:check`이다. 자동 테스트는 없고 실제 Apps Script 템플릿 평가와 런타임은 수동 검증 대상이다.
- `.clasp.json`은 로컬 전용이며 Script ID를 노출하거나 커밋하지 않는다. 배포 반영 명령은 검사 후 `clasp push`를 수행하는 `pnpm deploy`다.
- 에이전트는 요청이 없으면 코드 변경 후 `pnpm check`나 `clasp push`를 직접 실행하지 않는다.
- 날짜 계산은 시스템 로컬 시간 대신 `APP.TZ`와 `DateUtils.gs`를 사용하며 Apps Script와 스프레드시트 시간대를 `Asia/Seoul`로 유지한다.

## 코드 변경 원칙

- 공개 `google.script.run` 함수는 `publicCall_()`의 `{ ok, data/error }` 형식을 유지하고, 클라이언트 입력과 권한은 서버에서 다시 검증한다.
- 시트 쓰기는 `LockService`로 보호하고 잠금 후 최신 상태를 다시 읽는다. 기존 데이터나 알 수 없는 열을 자동 삭제·덮어쓰지 않는다.
- 화면과 사용자 메시지는 한국어로 유지하고 비밀번호·세션 토큰을 URL, 오류, 기록 시트에 남기지 않는다.

## 검증

- 스프레드시트의 `스공시 관리 → 시트 초기화 및 검사`를 실행한 뒤 실제 웹 앱에서 변경한 흐름을 점검한다.
- 날짜·출결 변경은 이 문서의 `핵심 테스트 항목`을 기준으로 검증한다.

# 스공시 좌석 및 출결 관리 시스템 구현 정책

## 1. 개요

스공시(스스로 공부하는 시간) 좌석 및 출결 관리 시스템을 Google Apps Script 웹 앱과 Google Spreadsheet로 구현한다.

- 학생은 학번과 전화번호 뒤 4자리로 로그인한다.
- 학생은 향후 결석 예정일을 등록하고 취소할 수 있다.
- 교사는 날짜와 교시별 좌석 및 출결 상태를 확인하고 결석 상태를 변경할 수 있다.
- 웹 앱은 링크를 아는 누구나 접근할 수 있도록 배포하고 배포자 권한으로 실행한다.
- 모든 날짜와 시각 계산은 `Asia/Seoul` 시간대를 기준으로 한다.

## 2. 프로젝트 구성

대상 스프레드시트에 연결된 컨테이너 바운드 Apps Script 프로젝트로 구성하고, 로컬에서는 `clasp`로 관리한다. Apps Script에 업로드되는 manifest, `.gs`, `.html` 파일은 `src/`에 모으고 `.clasp.json`의 `rootDir`도 `src`로 설정한다.

로컬 개발 도구는 pnpm으로 관리한다. `pnpm typecheck`는 Apps Script 타입을 사용해 `.gs` 파일을 검사한다. `pnpm lint`는 `.gs` ESLint 검사 후 Apps Script 템플릿 표현식을 전처리한 HTML 구조 및 페이지별 인라인 JavaScript 검사를 실행한다. `pnpm check`는 타입 검사와 전체 린트를 해당 순서로 실행하며, `pnpm deploy`는 검사를 통과한 뒤 `clasp push`를 수행한다. 실제 Apps Script 템플릿 평가와 런타임은 배포 후 수동으로 검증한다.

예상 파일 구성은 다음과 같다.

- `src/appsscript.json`: V8 런타임, 권한 및 시간대 설정
- `src/Code.gs`: 웹 라우팅과 초기화
- `src/Auth.gs`: 학생 및 교사 인증과 세션 관리
- `src/SheetService.gs`: 시트 생성, 검증, 출결 데이터 읽기와 쓰기
- `src/AttendanceService.gs`: 결석 등록, 복원 및 자동 출석 처리
- `src/TriggerService.gs`: 일일 트리거 설치 및 누락 처리 보충
- `src/Index.html`: 메인 화면
- `src/Student.html`: 학생 화면
- `src/Teacher.html`: 교사 화면
- 공통 CSS 및 클라이언트 JavaScript HTML 조각

페이지는 쿼리스트링으로 구분한다.

- 쿼리 없음: 메인 페이지
- `?page=student`: 학생 페이지
- `?page=teacher`: 교사 페이지

교사 비밀번호는 쿼리스트링이나 URL에 포함하지 않는다.

## 3. 시트 구성

시트가 없으면 `명부`, `기록`, `설정` 시트를 자동 생성한다. 기존 데이터와 알 수 없는 추가 열은 자동으로 삭제하거나 덮어쓰지 않는다.

### 3.1 명부 시트

1행과 2행을 헤더로 사용하고, 3행부터 학생 한 명당 한 행을 사용한다.

| 열 | 내용 |
| --- | --- |
| A | 학번 |
| B | 이름 |
| C | 좌석번호 |
| D | 전화번호 뒤 4자리 |
| E:G | 월요일 1~3교시 신청 여부 |
| H:J | 화요일 1~3교시 신청 여부 |
| K:M | 수요일 1~3교시 신청 여부 |
| N:P | 목요일 1~3교시 신청 여부 |
| Q | 내부 학생 키 |
| R 이후 | 날짜별 1~3교시 출결 |

신청 여부는 `1`이면 신청, `0` 또는 빈 값이면 미신청이다.

내부 학생 키는 Apps Script가 UUID로 생성하며 Q열은 숨김 처리한다. 화면과 다른 시트에서는 학번만으로 학생을 식별하지 않고 내부 학생 키를 함께 사용한다.

출결 열은 날짜마다 3개씩 생성한다.

- 1행: 연도를 포함한 실제 날짜값. 시트 표시 형식은 `M/d`로 설정한다.
- 2행: `1교시`, `2교시`, `3교시`
- 날짜 열은 연도까지 포함한 날짜값을 기준으로 오래된 날짜부터 시간순으로 배치한다.
- 필요한 날짜 열을 추가할 때 시간순 위치에 3개 열을 삽입하며, 기존 출결값과 헤더가 함께 이동하도록 한다.
- 연도가 달라도 같은 월과 일이 충돌하지 않는다.

한번 추가한 학생 행의 학번과 이름은 수정하지 않는다. 수정이 필요하면 기존 학생 행의 12개 신청값을 모두 비우고 수정된 정보로 새 학생 행을 추가한다. 신청 교시가 모두 빈 행은 비활성 학생으로 취급하며, 과거 출결은 기존 내부 학생 키를 계속 참조한다.

### 3.2 기록 시트

학생이나 교사가 수동으로 수행한 상태 변경만 추가 기록한다. 자동 생성된 신청 상태 `1`은 기록하지 않는다.

기록 항목은 다음과 같다.

- 변경 시각
- 역할: 학생 또는 교사
- 내부 학생 키
- 학번
- 대상 날짜
- 교시
- 이전 상태
- 새 상태

학생 사전 결석 등록과 취소, 교사 출석·결석 처리와 복원을 기록한다. 로그인 성공과 실패는 기록하지 않는다.

### 3.3 설정 시트

미운영일만 관리한다.

| 열 | 내용 |
| --- | --- |
| A | 날짜 |
| B | 사유 |

1행은 헤더로 사용한다. 등록된 날짜는 학생 날짜 선택과 교사 출결 대상에서 제외한다. 일일 트리거 실행 시 오늘부터 `오늘+30일` 범위에 해당 날짜의 출결 열이 이미 있으면 삭제한다. 과거 출결 열은 삭제하지 않는다.

## 4. Script Properties

기존에 하드코딩할 예정이었던 운영 설정은 모두 Script Properties로 관리한다.

필수 키는 다음과 같다.

- `TEACHER_PASSWORD`
- `TOTAL_SEATS`
- `PERIOD_1_START_TIME`
- `PERIOD_2_START_TIME`
- `PERIOD_3_START_TIME`

초기 운영 권장값은 다음과 같다.

- `TOTAL_SEATS`: `20`
- `PERIOD_1_START_TIME`: `18:00`
- `PERIOD_2_START_TIME`: `19:20`
- `PERIOD_3_START_TIME`: `20:50`

Script Properties가 하나라도 없거나 형식이 잘못되면 학생과 교사 기능을 모두 차단한다. 교사 화면에는 누락되거나 잘못된 키 목록과 Apps Script 설정 화면에서 입력하는 방법을 안내한다. 설정값과 비밀번호 자체는 화면에 노출하지 않는다.

## 5. 출결 상태

명부 시트의 날짜별 출결 셀은 다음 값을 사용한다.

| 값 | 의미 | 교사 화면 색상 |
| --- | --- | --- |
| 빈 값 | 미래 미확정 또는 해당 교시 미신청 | 신청 학생은 흰색, 미신청은 회색 |
| `0` | 기존 데이터의 미확정 또는 미신청 값 | 빈 값과 동일 |
| `1` | 신청함 | 흰색 |
| `2` | 출석함 | 초록색 |
| `3` | 결석함 | 빨간색 |
| `4` | 학생이 등록한 사전 결석 | 주황색 |

색상 판정 규칙은 다음과 같다.

- 해당 날짜와 교시에 신청한 학생이 없으면 회색이다.
- 기존 출결값 `0`은 빈 값과 동일하게 읽되 새 출결값으로 기록하지 않는다.
- 신청 학생의 미래 출결 셀이 비어 있어도 신청 상태이므로 흰색이다.
- 출결값이 `1`이면 흰색이다.
- 출결값이 `2`이면 초록색이다.
- 출결값이 `3`이면 빨간색이다.
- 출결값이 `4`이면 주황색이다.

기존 출결값은 별도 마이그레이션 없이 위 의미로 재해석한다.

## 6. 운영일과 자동 출석

운영일은 미운영일로 등록되지 않은 월요일, 화요일, 수요일, 목요일이다.

운영일 시작 무렵에 Apps Script 일일 시간 기반 트리거가 다음 작업을 수행한다.

1. 오늘부터 `오늘+30일`까지 운영일의 출결 열이 없으면 날짜별 3개 열을 생성한다.
2. 같은 범위의 금~일 및 설정 시트에 등록된 미운영일은 출결 열이 있으면 3개 열을 삭제한다.
3. 과거 출결 열은 미운영일로 등록되어도 삭제하지 않는다.
4. 당일 해당 요일에 신청한 학생의 교시에 신청 상태 `1`을 기록한다.
5. 학생 또는 교사가 이미 `2`, `3`, `4`를 기록한 셀은 덮어쓰지 않는다.
6. 미신청 교시는 빈 값으로 둔다.
7. 자동 생성한 `1`은 기록 시트에 남기지 않는다.

Apps Script 시간 기반 트리거는 정확히 자정에 실행되지 않고 자정 근처에 실행될 수 있다. 웹 요청이 들어오면 당일 자동 처리가 누락되었는지 검사하고 필요한 경우 보충 처리한다. 당일 자동 처리 완료 표식이 있으면 명부와 출결 셀을 다시 읽지 않고 보충 처리를 종료한다.

일일 트리거가 미래 30일분의 운영일 출결 열을 한꺼번에 생성한다. 학생이나 교사가 트리거 실행 전에 특정 운영일을 처음 사용하면 필요한 해당 날짜 열을 생성한다. 새 날짜 열은 기존 날짜 열 사이의 시간순 위치에 삽입한다. 미래 날짜 사용은 연도가 바뀌는 경우를 포함해 한국 시간 기준 오늘부터 `오늘+30일`까지 허용한다.

## 7. 메인 페이지

메인 페이지에는 학생용과 교사용 선택 메뉴를 표시한다.

- 학생용 선택 시 학생 로그인 페이지로 이동한다.
- 교사용 선택 시 비밀번호 입력 화면을 표시한다.
- 교사 비밀번호는 서버에서 검증하며 URL에 넣지 않는다.
- 필수 Script Properties가 잘못된 경우 정상 메뉴 대신 설정 오류 안내를 표시한다.
- 학생용 또는 교사용 메뉴를 누르면 대상 페이지가 표시될 때까지 이동 대상과 로딩 상태를 안내하고 중복 클릭을 막는다.

## 8. 학생 기능

### 8.1 로그인

- 학번과 전화번호 뒤 4자리로 로그인한다.
- 전화번호 뒤 4자리 입력은 화면에서 마스킹한다.
- 전화번호 뒤 4자리는 앞자리 `0`을 포함하는 정확한 4자리 문자열로 정규화한다.
- 같은 학번의 활성 행이 둘 이상이거나 비밀번호 데이터 형식이 잘못되었으면 로그인을 차단하고 오류를 안내한다.
- 같은 학번의 비활성 과거 행이 존재하더라도 활성 행이 하나뿐이면 해당 활성 행으로 로그인한다.
- 페이지를 새로고침하면 다시 로그인해야 한다.
- 로그인한 학생 키와 만료 시각은 브라우저의 JavaScript 변수에만 보관하며 localStorage, sessionStorage, 쿠키 또는 서버 세션에 저장하지 않는다.
- 학생 로그인은 로그인 시점부터 5분 후 만료한다. 활동으로 만료 시각을 연장하지 않으며, 만료 시 로그인 화면으로 돌아간다. 이 만료는 클라이언트 화면에서만 적용하고 서버는 내부 학생 키로 활성 학생 여부를 다시 검증한다.
- 로그아웃하면 페이지를 새로고침하지 않고 학생 로그인 화면으로 돌아간다.
- 별도의 로그인 시도 횟수 제한은 적용하지 않는다.

### 8.2 사전 결석 등록

- 하루 또는 날짜 범위를 선택할 수 있다.
- 선택 가능 범위는 오늘부터 `오늘+30일`까지다.
- 선택 범위 중 월~목 운영일만 적용한다.
- 금~일 및 설정 시트의 미운영일은 조용히 제외한다.
- 적용 가능한 운영일이 없으면 오류를 표시한다.
- 처리 후 실제 적용된 날짜 목록과 건수를 표시한다.
- 각 운영일에서 학생 본인이 신청한 모든 교시를 `4`로 변경한다.
- 이미 교사가 `2`나 `3`으로 처리한 교시는 학생이 변경할 수 없다.

로그인 후 본인이 등록한 사전 결석 내역을 날짜순으로 조회할 수 있다. 목록에는 날짜별 선택 항목과 현재 취소 가능 여부만 표시하고 교시는 표시하지 않으며, 오늘부터 `오늘+30일` 범위의 상태 `4`를 조회 대상으로 한다. 취소할 수 있는 날짜를 여러 개 선택해 한 요청으로 취소하며, 선택한 날짜가 없으면 일괄 취소 버튼을 비활성화한다. 전체 선택 버튼은 취소 가능한 날짜만 모두 선택하고, 모두 선택된 상태에서는 같은 버튼이 전체 해제로 동작한다.

### 8.3 결석 취소

- 학생은 본인이 만든 사전 결석 `4`만 취소할 수 있다.
- 선택한 여러 날짜는 잠금 안에서 한 번에 다시 검증하고 하나의 출결 범위와 기록 묶음으로 저장한다.
- 교사가 만든 출석 `2`와 결석 `3`은 취소할 수 없다.
- 미래 날짜의 `4`를 취소하면 원래의 빈 값으로 복원한다.
- 당일 자동 신청 상태 생성 이후 `4`를 취소하면 `1`로 복원한다.

### 8.4 당일 마감

`PERIOD_1_START_TIME`을 당일 학생 변경 마감 시각으로 함께 사용한다.

- 기본값은 18:00이다.
- 18:00 전에는 당일 등록과 취소가 가능하다.
- 18:00부터는 당일 모든 교시의 등록과 취소를 금지한다.
- 마감 후에는 미래 날짜만 변경할 수 있다.
- 당일 마감 후 학생 화면의 시작일과 종료일 최소값 및 기본값은 내일로 표시한다.

## 9. 교사 인증

- 메인 페이지에서 교사 비밀번호를 입력한다.
- 서버가 Script Properties의 `TEACHER_PASSWORD`와 비교한다.
- 성공하면 추측할 수 없는 임의 세션 토큰을 발급한다.
- 브라우저에는 원본 세션 토큰을 저장하고, 서버의 Script Properties에는 토큰 해시와 한국 시간 기준 만료일만 저장한다. 토큰 원문은 서버에 저장하지 않으며, 인증과 신규 로그인 시 만료된 세션 속성을 정리한다.
- 토큰은 브라우저를 닫았다 열어도 한국 시간 기준 같은 날까지 유지한다.
- 한국 시간 자정이 지나면 활동 중이어도 다시 인증해야 한다.
- 로그아웃 기능을 제공한다.
- 교사 페이지에서 메인 화면으로 이동할 때도 브라우저 토큰과 서버 세션을 정리해 로그아웃한다.
- URL, 쿼리스트링, 오류 메시지 및 기록 시트에 비밀번호를 남기지 않는다.
- 로그인 실패 횟수 제한은 적용하지 않는다.

## 10. 교사 화면

교사 페이지는 최초 인증과 설정 확인이 끝날 때까지 빈 화면 대신 `불러오는 중...`을 표시한다. 좌석 데이터를 조회하는 동안에는 좌석 선택, 상세 정보 열기와 일괄 변경을 할 수 없도록 좌석 영역 전체의 상호작용을 차단한다.

### 10.1 날짜와 교시

- 기본 날짜는 가장 최근 운영일이다.
- 시트에 존재하는 모든 과거 출결 날짜를 조회할 수 있다.
- 미래는 오늘부터 `오늘+30일`까지 조회하고 수정할 수 있다.
- 날짜와 교시를 직접 선택할 수 있다.
- 미운영일은 출결 대상에서 제외한다.

현재 시각에 따른 기본 교시는 다음과 같다.

- 19:20 이전: 1교시
- 19:20 이상 20:50 이전: 2교시
- 20:50 이상: 3교시

18:00 이전에도 기본 선택은 1교시다. 정확히 19:20과 20:50에는 다음 교시로 넘어간다.

### 10.2 좌석 배치

- 총 좌석 수는 `TOTAL_SEATS`를 사용하며 초기값은 20석이다.
- 좌석번호는 `1..TOTAL_SEATS`의 연속된 정수다.
- 좌우에 각각 3열씩 배치하여 총 6열로 표시한다.
- 중앙에 통로 간격을 둔다.
- 좌석 두 행마다 추가 세로 간격을 둔다.
- 좌석번호는 왼쪽 위부터 행 우선으로 증가한다.
- 학생이 없는 좌석도 물리적 좌석으로 표시한다.
- 학생이 있으면 좌석번호, 학번, 이름을 표시한다.
- 데스크톱 화면을 우선하되 학생 화면과 교사 화면 모두 모바일에서 사용할 수 있어야 한다.
- 교사 모바일 화면은 6열 구조를 축소하지 않고 가로 스크롤을 허용한다.

### 10.3 좌석 선택과 상세 정보

좌석 본문을 누르면 여러 좌석을 선택하거나 선택 해제한다. 선택 좌석은 색상 이외의 테두리와 체크 표시로도 구분한다. 좌석 오른쪽 위의 작은 정보 아이콘을 누르면 다음 내용을 팝업으로 표시한다.

- 학번
- 이름
- 좌석번호
- 월~목 전체 신청 요일 및 교시. 모든 교시 신청은 `월`, 일부 교시 신청은 `수1-2`, `목1` 형식으로 표시
- 현재 출결 상태

좌석 배치 상단에는 선택 인원과 출석, 결석, 되돌리기, 전체 선택 버튼을 항상 표시하고, 좌석을 스크롤하는 동안에도 화면에 고정한다. 전체 선택 버튼은 현재 표시된 학생 좌석을 모두 선택하고, 모두 선택된 상태에서는 같은 버튼이 전체 해제로 동작한다. 출석, 결석, 되돌리기는 선택 인원이 없으면 비활성화한다. 변경은 `google.script.run`을 통해 새로고침 없이 선택 좌석에 일괄 처리한다. 처리 중에는 중복 클릭을 막고, 성공하면 최신 좌석 상태를 갱신하고 선택을 해제한다.

## 11. 교사 오버라이드

### 11.1 출석 및 결석 처리

- 출석은 선택한 교시를 `2`, 결석은 `3`으로 변경한다.
- 같은 날의 이후 교시 중 선택 학생이 신청한 교시도 같은 상태로 변경한다.
- 여러 학생을 선택하면 한 요청에서 일괄 적용한다.
- 과거, 오늘 및 향후 30일 이내 날짜에 적용할 수 있다.
- 각 변경의 이전 상태와 새 상태를 기록 시트에 남긴다.

### 11.2 복원

- 선택한 교시와 같은 날의 이후 신청 교시를 모두 신청 상태 `1`로 초기화한다.
- 기록 시트의 이전 상태는 조회하지 않는다.
- 복원 내역도 기록 시트에 남긴다.

## 12. 데이터 검증

다음 오류를 검사한다.

- 학번 중복
- 전화번호 뒤 4자리 형식 오류
- 내부 학생 키 누락 또는 중복
- 좌석번호가 `1..TOTAL_SEATS` 범위를 벗어남
- 신청값이 `0`, `1`, 빈 값 이외임
- 같은 요일과 같은 교시에 두 학생이 같은 좌석을 신청함
- 필수 시트나 헤더 구조가 잘못됨
- Script Properties가 누락되거나 형식이 잘못됨
- 날짜 헤더가 중복되거나 시간순으로 정렬되지 않음
- 날짜별 교시 열이 정확히 3개로 구성되지 않음
- 출결값이 빈 값, `0`, `1`, `2`, `3`, `4` 이외임

오류 처리 원칙은 다음과 같다.

- 오류가 있는 학생만 교사 좌석 배치에서 제외한다.
- 교사 화면 상단에 학번, 요일, 교시, 좌석번호 등 구체적인 오류를 표시한다.
- 오류와 관계없는 학생과 좌석은 정상 표시한다.
- 좌석 충돌 학생도 학생 페이지에서 사전 결석을 등록할 수 있다.
- 잘못된 값을 자동 보정하거나 첫 번째 학생만 조용히 표시하지 않는다.
- 기존 시트의 잘못된 헤더나 데이터를 자동으로 덮어쓰지 않는다.

학번 중복은 활성 학생 행만 대상으로 판정한다. 신청 교시 12개가 모두 빈 비활성 과거 행은 같은 학번이 있어도 오류로 보지 않으며, 같은 학번의 활성 행이 둘 이상일 때만 오류로 처리한다.

## 13. 시트 초기화

실행 시 다음 정책을 적용한다.

1. `명부`, `기록`, `설정` 시트가 없으면 생성한다.
2. 빈 시트이면 정해진 헤더를 생성한다.
3. 첫 번째 기존 시트의 이름이 다르고 완전히 비어 있으면 `명부`로 이름을 변경할 수 있다.
4. 기존 데이터나 알 수 없는 추가 열은 삭제하지 않는다.
5. 필수 헤더가 잘못되었으면 자동 덮어쓰기 대신 교사 화면에 오류를 안내한다.
6. 내부 학생 키가 없는 기존 학생 행에는 UUID를 생성하되, 쓰기 전 잠금을 획득하고 중복 여부를 검사한다.

## 14. 동시성과 보안

- 모든 시트 쓰기 작업에 `LockService`를 사용한다.
- 잠금을 획득한 뒤 최신 상태를 다시 읽고 변경한다.
- 한 요청에서 같은 미운영일 목록과 출결 날짜 열을 반복 조회하지 않으며, 같은 학생의 3개 교시 출결과 여러 기록 행은 가능한 한 범위 단위로 읽고 쓴다.
- 학생 출결 변경 후 화면 데이터를 만들 때 전체 명부를 다시 읽지 않고 잠금 안에서 해당 학생의 최신 출결 행만 다시 읽는다.
- 같은 날짜 열이나 내부 학생 키가 중복 생성되지 않도록 한다.
- 학생의 여러 결석일 일괄 취소는 선택 날짜를 포함하는 해당 학생의 출결 행 구간을 한 번 읽고 한 번 쓴다.
- 날짜 열을 추가할 때 기존 열을 시간순으로 다시 확인하고 올바른 위치에 3개 열을 삽입한다.
- 서버에서 날짜 범위, 운영일, 역할, 학생 소유권 및 상태 전환 가능 여부를 다시 검증한다.
- 클라이언트가 전달한 이름, 좌석번호, 기존 상태 및 권한 정보는 신뢰하지 않는다.
- 교사 세션 토큰은 서버에서 날짜와 함께 검증한다.
- 동시 변경 충돌 시 최신 서버 상태를 반환하여 화면을 다시 갱신한다.
- 사용자에게 내부 오류나 비밀번호 등 민감한 값을 노출하지 않는다.

## 15. UI 원칙

- 모든 화면과 메시지는 한국어로 제공한다.
- 학생 화면은 휴대전화 우선으로 설계한다.
- 교사 화면은 데스크톱 좌석 배치를 우선한다.
- 버튼과 입력 요소는 모바일에서도 누르기 충분한 크기로 만든다.
- 출결 상태는 색상뿐 아니라 텍스트나 레이블로도 구분하여 접근성을 확보한다.
- 서버 요청 중 로딩 상태를 표시하고 중복 제출을 막는다.
- 학생·교사 페이지에서 메인으로 이동할 때도 새 페이지가 표시될 때까지 로딩 상태를 안내한다.
- 성공과 실패 결과를 페이지 안에서 명확히 안내한다.

## 16. 구현 순서

1. `clasp` 프로젝트와 Apps Script manifest를 구성한다.
2. 시트 자동 생성과 헤더 검증을 구현한다.
3. Script Properties 검증과 설정 오류 안내를 구현한다.
4. 내부 학생 키 생성과 명부 데이터 검증을 구현한다.
5. 날짜별 출결 열 생성과 자동 출석 처리를 구현한다.
6. 일일 트리거 설치 함수와 웹 요청 시 보충 처리를 구현한다.
7. 학생 인증과 사전 결석 등록, 조회 및 취소를 구현한다.
8. 교사 인증과 당일 세션 관리를 구현한다.
9. 교사 날짜 및 교시 조회와 좌석 배치를 구현한다.
10. 교사 결석 전파, 복원 및 수동 변경 기록을 구현한다.
11. 동시성 제어, 오류 처리 및 반응형 UI를 완성한다.
12. 경계 시각, 미운영일, 날짜 범위, 좌석 중복 및 상태 복원을 테스트한다.
13. `clasp` 배포와 웹 앱 운영 및 데이터 보관 절차를 문서화한다.

## 17. 핵심 테스트 항목

- 빈 스프레드시트에서 3개 시트와 헤더가 정상 생성되는지 확인한다.
- 필수 Script Properties 누락 시 전체 기능이 차단되는지 확인한다.
- `0123` 같은 전화번호 뒤 4자리로 로그인할 수 있는지 확인한다.
- 학생 로그인 5분 후 자동으로 로그인 화면으로 돌아가고, 활동해도 만료 시각이 연장되지 않는지 확인한다.
- 학생 페이지를 새로고침하면 학생 키와 만료 시각이 사라져 다시 로그인해야 하는지 확인한다.
- 학생이 오늘부터 30일 후까지 날짜 또는 범위를 선택할 수 있는지 확인한다.
- 학생이 본인의 사전 결석 목록과 취소 가능 여부를 날짜순으로 조회할 수 있는지 확인한다.
- 학생이 여러 사전 결석 날짜를 선택해 한 번에 취소하고, 취소 불가 날짜는 선택할 수 없는지 확인한다.
- 학생 전체 선택 버튼이 취소 가능한 날짜만 선택하고 같은 버튼으로 전체 해제되는지 확인한다.
- 주말과 미운영일이 범위에서 제외되는지 확인한다.
- 18:00 전후로 당일 학생 변경 권한이 정확히 달라지는지 확인한다.
- 19:20과 20:50 경계에서 기본 교시가 바뀌는지 확인한다.
- 자정 자동 처리에서 신청 교시만 `1`이 되고 기존 `2`, `3`, `4`가 보존되는지 확인한다.
- 자정 자동 처리에서 오늘부터 30일 후까지 운영일 열이 생성되고 같은 범위의 미운영일 열이 삭제되는지 확인한다.
- 미운영일로 등록된 과거 날짜의 출결 열은 삭제되지 않는지 확인한다.
- 미래의 빈 신청 학생 출결 셀이 교사 화면에서 흰색 신청함으로 표시되는지 확인한다.
- 여러 좌석의 교사 출석·결석 처리가 같은 날 이후 신청 교시에 일괄 전파되는지 확인한다.
- 교사 전체 선택 버튼이 현재 표시된 학생 좌석을 모두 선택하고 같은 버튼으로 전체 해제되는지 확인한다.
- 교사 되돌리기 시 기록 조회 없이 이후 신청 교시가 모두 `1`로 초기화되는지 확인한다.
- 학생이 교사 출석 `2`나 결석 `3`을 취소할 수 없는지 확인한다.
- 같은 교시 좌석 중복 학생이 좌석에서 제외되고 오류로 표시되는지 확인한다.
- 동시에 같은 출결을 수정해도 중복 열이나 잘못된 상태가 생기지 않는지 확인한다.
- 교사 세션이 한국 시간 자정에 만료되는지 확인한다.
- 자동 생성된 `1`은 기록 시트에 남지 않고 수동 변경만 기록되는지 확인한다.
- 같은 학번의 비활성 과거 행이 있어도 활성 행 하나로 정상 로그인되는지 확인한다.
- 연도가 다른 동일한 월과 일의 출결 열이 충돌하지 않는지 확인한다.
- 날짜를 순서와 다르게 요청해도 날짜 열이 항상 시간순으로 배치되는지 확인한다.
- 출결값 `0`이 빈 값과 동일하게 처리되고 새 값으로 기록되지 않는지 확인한다.
- 트리거 설치 함수를 반복 실행해도 일일 트리거가 하나만 존재하는지 확인한다.

## 18. 운영 절차

- 최초 배포 전에 Script Properties의 모든 필수 값을 설정한다.
- 일일 자동 출석 트리거 설치 함수를 관리자가 한 번 실행하고 권한을 승인한다. 설치 함수는 동일한 처리 함수를 실행하는 기존 트리거를 확인하여 중복 생성하지 않고 하나만 유지한다.
- Apps Script 프로젝트와 스프레드시트 시간대를 `Asia/Seoul`로 맞춘다.
- 명부의 학번과 이름은 직접 수정하지 않고, 변경 시 기존 신청값을 비운 후 새 행을 추가한다.
- 미운영일은 설정 시트에 실제 날짜값으로 등록한다.
- 필요하면 연도별로 스프레드시트를 복사해 보관할 수 있지만, 날짜 헤더에 연도가 포함되므로 새해에 출결 열을 삭제할 필요는 없다.
- 웹 앱은 새 버전 배포 후 실제 학생 및 교사 계정 흐름으로 점검한다.
